<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.group0578.hpgame"
    tools:ignore="GoogleAppIndexingWarning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".level1.FlyingTransitionActivity" />
        <activity android:name=".level1.FlyingActivity" />
        <activity android:name=".view.CreateUserActivity" />
        <activity android:name=".view.CreateUser" />
        <activity android:name=".view.StartupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".level2.MazeActivity" />
        <activity android:name=".level3.Level3MainActivity" />
        <activity android:name=".view.ProfilePageActivity" />
        <activity android:name=".view.CustomizeActivity" />
        <activity android:name=".view.GameOverActivity" />
    </application>

</manifest>